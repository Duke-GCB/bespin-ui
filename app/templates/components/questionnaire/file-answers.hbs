{{#if readOnly}}
  {{#each answerValues as |answerValue|}}
    {{questionnaire.string-answer answerValue readOnly}}
  {{/each}}
{{else}}
  {{questionnaire.dds-file-answers answerValues}}
{{/if}}
{{yield}}
