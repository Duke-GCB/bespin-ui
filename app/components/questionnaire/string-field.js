import AnswerableField from './answerable-field';

const StringField = AnswerableField.extend({
  /**
   * Encapsulates a text field for a single string answer
   * JS just uses the base AnswerableField
   */
});

export default StringField;
