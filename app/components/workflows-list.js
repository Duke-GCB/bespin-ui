import Ember from 'ember';


const WorkflowsListComponent = Ember.Component.extend({
});

WorkflowsListComponent.reopenClass({
  positionalParams: ['workflows']
});

export default WorkflowsListComponent;
